name: Build and deploy

on:
  push:
    branches:
      - master

jobs:
  github-pages:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        env:
          JEKYLL_PAT: ${{ secrets.JEKYLL_PAT }}

